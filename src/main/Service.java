package main;

//
// Ainda em fase de implementação
//
public class Service{
	
    public void serviceMain(String[] args)  {
    	
    	/*
    	beanScheduler bs;
    	scheduler oscheduler = new scheduler();
    	
    	
    	EventLog.report("ETLExtractor: início do serviço", EventLog.INFORMATION, "Ping");
    	
    	while (!shutdown) {
            try {
            	//oscheduler.start();
            } catch (Exception e) {
            	EventLog.report("ETLExtractor: "+e.toString(), EventLog.ERROR, "Ping");
            }
        }
    	beanScheduler.state = beanScheduler.ISSTOP;
    	EventLog.report("ETLExtractor: fim do serviço", EventLog.INFORMATION, "Ping");
        return 0;
        */
    }

}
